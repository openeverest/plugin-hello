apiVersion: v1
kind: Service
metadata:
  name: {{ include "plugin-hello.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "plugin-hello.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "plugin-hello.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
